@model IEnumerable<PIA_PRUEBA.Models.Active>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.vActive)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.vActive)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tActive_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.tActive_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tActive_ID })
        </td>
    </tr>
}

</table>
