@model IEnumerable<PIA_PRUEBA.Models.City>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.vCity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Country.vCountry)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.vCity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Country.vCountry)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.iCity_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.iCity_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.iCity_ID })
        </td>
    </tr>
}

</table>
