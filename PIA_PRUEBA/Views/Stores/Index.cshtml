@model IEnumerable<PIA_PRUEBA.Models.Store>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Address.vAddress)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Address.vAddress)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.iStore_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.iStore_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.iStore_ID })
        </td>
    </tr>
}

</table>
